import React from 'react';
import { AiOutlineAim as TargetIcon } from 'react-icons/ai';

const GeolocateButton = () => {
  return (
    <button type="button" onClick={() => {}}>
      <TargetIcon />
    </button>
  );
};

export default GeolocateButton;
